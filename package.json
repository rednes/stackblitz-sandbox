{
  "name": "stackblitz-sandbox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "run-p backend frontend",
    "frontend": "npm run -w frontend dev",
    "backend": "npm run -w backend dev",
    "lint": "npm run -w frontend lint",
    "lint:fix": "npm run -w frontend lint:fix"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
